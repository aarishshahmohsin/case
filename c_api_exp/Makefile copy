SRC = scip_solver.c

OUT = libscip_solver.so

# Flags (adjust if SCIP is installed elsewhere)
CFLAGS = -fPIC -O3 -I/path/to/scip/include
LDFLAGS = -shared -L/path/to/scip/lib -lscip -lm

all: $(OUT)

$(OUT): $(SRC)
	$(CC) $(CFLAGS) -o $(OUT) $(SRC) $(LDFLAGS)

clean:
	rm -f $(OUT)
